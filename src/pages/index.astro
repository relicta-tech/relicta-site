---
import SiteLayout from "../layouts/SiteLayout.astro";

const title = "Relicta — The governance layer for software change";
const description = "Relicta governs change in an AI-driven world. From release automation to risk-aware decision making for humans and agents.";
---
<SiteLayout {title} {description}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-text">
      <p class="eyebrow">The governance layer for software change</p>
      <h1>Governing change in an AI-driven world</h1>
      <p class="lede">As AI agents and CI systems generate more code, deciding what should ship becomes the hardest problem. Relicta governs change — before it reaches production.</p>
      <div class="hero-actions">
        <a class="btn solid" href="https://docs.relicta.tech/install">Get started with the CLI</a>
        <a class="btn ghost" href="/vision">Read the vision</a>
      </div>
    </div>
    <div class="hero-card">
      <div class="terminal">
        <div class="abstract-diagram">
          <div class="diagram-node">Agent / CI</div>
          <div class="diagram-arrow">&rarr;</div>
          <div class="diagram-node node-relicta">Relicta</div>
          <div class="diagram-arrow">&rarr;</div>
          <div class="diagram-node">Production</div>
        </div>
      </div>
      <div class="card-foot">The decision layer for modern software delivery.</div>
    </div>
  </section>

  <!-- Numbers Bar -->
  <section class="numbers-bar reveal">
    <div class="number-item">
      <span class="number-value">7</span>
      <span class="number-label">Risk factors with historical learning</span>
    </div>
    <div class="number-item">
      <span class="number-value">5</span>
      <span class="number-label">AI providers supported</span>
    </div>
    <div class="number-item">
      <span class="number-value">4</span>
      <span class="number-label">Audience-specific narratives</span>
    </div>
    <div class="number-item">
      <span class="number-value">1</span>
      <span class="number-label">Binary. Zero dependencies.</span>
    </div>
  </section>

  <!-- The Problem -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">The Problem</p>
      <h2>Software delivery scaled. Decision-making didn't.</h2>
      <p class="lede">Code is cheap. Changes are frequent. Risk is hidden. Trust erodes.</p>
    </div>
    <div class="grid">
      <div class="card">
        <h3>Past</h3>
        <p>Human-written code. Human reviews. Human pace.</p>
      </div>
      <div class="card">
        <h3>Now</h3>
        <p>Agent-generated change. CI automation. Overwhelmed humans.</p>
      </div>
      <div class="card card-featured">
        <h3>The Gap</h3>
        <p>Governance. How do we trust the machine to ship?</p>
      </div>
    </div>
  </section>

  <!-- What Relicta Is -->
  <section class="section reveal">
    <div class="grid">
      <div>
        <div class="section-heading">
          <p class="eyebrow">The Solution</p>
          <h2>Relicta governs change — not just releases.</h2>
        </div>
        <p class="lede">Relicta sits between humans, agents, and CI systems. It decides what changes, how risky it is, who approves, and how it's communicated.</p>
      </div>
      <div class="feature-list">
        <div>
          <h4>The Core Decisions</h4>
          <ul>
            <li>What changed? (Semantic Analysis)</li>
            <li>Is it risky? (Risk Scoring)</li>
            <li>Who approves? (Governance)</li>
            <li>How to tell users? (Communication)</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">How It Works</p>
      <h2>From commit to production, governed.</h2>
      <p class="lede">One command. Five stages. Full audit trail.</p>
    </div>
    <div class="pipeline">
      <div class="pipeline-step">
        <div class="pipeline-icon">1</div>
        <h4>Plan</h4>
        <p>Analyze commits, detect breaking changes, calculate risk score, suggest version.</p>
      </div>
      <div class="pipeline-connector"></div>
      <div class="pipeline-step">
        <div class="pipeline-icon">2</div>
        <h4>Bump</h4>
        <p>Apply semantic version. Create git tag.</p>
      </div>
      <div class="pipeline-connector"></div>
      <div class="pipeline-step">
        <div class="pipeline-icon">3</div>
        <h4>Notes</h4>
        <p>Generate AI-powered release notes. Engineering, product, executive, and customer variants.</p>
      </div>
      <div class="pipeline-connector"></div>
      <div class="pipeline-step">
        <div class="pipeline-icon">4</div>
        <h4>Approve</h4>
        <p>Policy check. Auto-approve low-risk, require humans for high-risk. Audit hash chain entry.</p>
      </div>
      <div class="pipeline-connector"></div>
      <div class="pipeline-step step-accent">
        <div class="pipeline-icon">5</div>
        <h4>Publish</h4>
        <p>Tag, changelog, plugins, Sigstore attestation. Done.</p>
      </div>
    </div>
    <div class="terminal pipeline-terminal">
      <pre><code><span class="t-muted">$</span> relicta release

<span class="t-step">plan</span>    47 commits since v1.3.0 &middot; 2 breaking, 8 features, 12 fixes
        Risk: <span class="t-warn">0.58</span> (medium) &middot; Blast radius: 3/7 packages
        Suggesting: <span class="t-accent">v2.0.0</span> (major)

<span class="t-step">bump</span>    Version set to v2.0.0

<span class="t-step">notes</span>   Release notes generated via Claude
        4 audience variants: engineering, product, executive, external

<span class="t-step">approve</span> Policy: risk > 0.5 requires approval
        Auto-approved: actor trust score 0.94
        Audit: SHA-256 hash chain entry recorded

<span class="t-step">publish</span> <span class="t-accent">&check;</span> GitHub release created
        <span class="t-accent">&check;</span> npm package published
        <span class="t-accent">&check;</span> Slack #releases notified
        <span class="t-accent">&check;</span> in-toto attestation signed via Sigstore

<span class="t-accent">Done.</span> v2.0.0 released with full governance trail.</code></pre>
    </div>
  </section>

  <!-- What Only Relicta Does -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Differentiators</p>
      <h2>What no other tool offers</h2>
      <p class="lede">Other tools automate releases. Relicta governs them.</p>
    </div>
    <div class="diff-grid">
      <article class="diff-card">
        <div class="diff-number">01</div>
        <h3>Change Governance Protocol</h3>
        <p>The first standardized, vendor-neutral protocol for governing software change. Actor-agnostic, transport-independent, audit-first.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">02</div>
        <h3>MCP-Native AI Agents</h3>
        <p>The only release tool that's an MCP server. AI agents can plan, assess risk, approve, and publish releases natively.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">03</div>
        <h3>Risk Scoring with Memory</h3>
        <p>7-factor weighted risk calculation that learns from past releases and incidents. Gets smarter every time you ship.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">04</div>
        <h3>Blast Radius Analysis</h3>
        <p>Maps changed files to impacted packages, builds dependency graphs, and quantifies change scope before you release.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">05</div>
        <h3>Audience-Specific Narratives</h3>
        <p>One release, four stories. Engineering gets the diff. Product gets highlights. Executives get impact. Customers get the upgrade guide.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">06</div>
        <h3>Policy DSL</h3>
        <p>Composable governance rules with full logic support. Define approval gates, team routing, and auto-approval criteria as code.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">07</div>
        <h3>Release Memory</h3>
        <p>Tracks actor reliability, incident correlation, and risk patterns over time. Your release intelligence compounds.</p>
      </article>
      <article class="diff-card">
        <div class="diff-number">08</div>
        <h3>SLSA Governance Attestation</h3>
        <p>in-toto v1 attestation signed via Sigstore. Proves not just what was built, but why it was approved and by whom.</p>
      </article>
    </div>
  </section>

  <!-- Comparison Table -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Comparison</p>
      <h2>Why Relicta?</h2>
    </div>
    <div class="compare-table-wrap">
      <table class="compare-table">
        <thead>
          <tr>
            <th>Capability</th>
            <th>semantic-release</th>
            <th>release-please</th>
            <th>goreleaser</th>
            <th>LaunchDarkly</th>
            <th class="col-relicta">Relicta</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Semver automation</td>
            <td class="check">&check;</td>
            <td class="check">&check;</td>
            <td class="check">&check;</td>
            <td class="dash">&mdash;</td>
            <td class="check accent">&check;</td>
          </tr>
          <tr>
            <td>AI release notes</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="accent">5 providers</td>
          </tr>
          <tr>
            <td>Risk scoring</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td>Basic</td>
            <td class="accent">7-factor + learning</td>
          </tr>
          <tr>
            <td>Approval workflows</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="accent">Policy DSL</td>
          </tr>
          <tr>
            <td>Audit trail</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td>Flags</td>
            <td class="accent">Cryptographic</td>
          </tr>
          <tr>
            <td>MCP server</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="accent">Native</td>
          </tr>
          <tr>
            <td>Blast radius</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="accent">&check;</td>
          </tr>
          <tr>
            <td>Supply chain attestation</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td>Partial</td>
            <td class="dash">&mdash;</td>
            <td class="accent">in-toto + Sigstore</td>
          </tr>
          <tr>
            <td>Single binary</td>
            <td class="dash">&mdash;</td>
            <td class="dash">&mdash;</td>
            <td class="check">&check;</td>
            <td class="dash">&mdash;</td>
            <td class="check accent">&check;</td>
          </tr>
          <tr>
            <td>Open source</td>
            <td class="check">MIT</td>
            <td class="check">Apache</td>
            <td class="check">MIT</td>
            <td class="dash">&mdash;</td>
            <td class="accent">MIT</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Trust & Security -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Trust & Security</p>
      <h2>Enterprise-grade security. Developer-grade UX.</h2>
    </div>
    <div class="trust-grid">
      <div class="trust-item">
        <h4>Zero data leaves your environment</h4>
        <p>Runs locally or in your CI. No SaaS. No cloud dependency. Your code, your keys, your infrastructure.</p>
      </div>
      <div class="trust-item">
        <h4>Cryptographic audit trails</h4>
        <p>Every governance decision is recorded in an immutable SHA-256 hash chain. Tamper-evident by design.</p>
      </div>
      <div class="trust-item">
        <h4>SLSA attestation</h4>
        <p>in-toto v1 statements signed via Sigstore prove what was released, why it was approved, and by whom.</p>
      </div>
      <div class="trust-item">
        <h4>Plugin sandboxing</h4>
        <p>Plugins run in isolated processes with capability-based restrictions on filesystem, network, and environment access.</p>
      </div>
      <div class="trust-item">
        <h4>SBOM generation</h4>
        <p>Full software bill of materials for supply chain transparency and compliance.</p>
      </div>
      <div class="trust-item">
        <h4>Secret masking</h4>
        <p>API keys and tokens are automatically redacted from all output and logs. Enabled by default.</p>
      </div>
    </div>
  </section>

  <!-- Production Ready -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Production Ready</p>
      <h2>Ship today. Govern tomorrow.</h2>
      <p class="lede">Start with release automation. Grow into full change governance.</p>
    </div>
    <div class="grid">
      <div class="feature-list">
        <ul>
          <li><strong>Semantic version planning</strong> from conventional commits — with heuristic fallback for messy histories</li>
          <li><strong>AI-assisted release notes</strong> via OpenAI, Anthropic, Gemini, Azure, or Ollama</li>
          <li><strong>Approval & audit trail</strong> with configurable policy DSL</li>
          <li><strong>Plugin-based publishing</strong> — GitHub, npm, Docker, Slack, Jira, Helm, and more</li>
          <li><strong>MCP server for AI agents</strong> — full release workflow via Claude, GPT, or custom agents</li>
          <li><strong>Monorepo & multi-repo</strong> — independent, lockstep, or hybrid versioning with dependency coordination</li>
        </ul>
        <div class="hero-actions">
           <a class="btn solid" href="https://docs.relicta.tech/install">Install Relicta</a>
           <a class="btn ghost" href="https://github.com/relicta-tech/relicta">View GitHub</a>
        </div>
      </div>
      <div class="terminal">
        <img src="/demos/marketing.gif" alt="Relicta CLI usage" loading="lazy" />
      </div>
    </div>
  </section>

  <!-- From Today -> Vision -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">The Evolution</p>
      <h2>From release automation to change governance</h2>
    </div>
    <div class="comparison-table">
      <div class="table-header">
        <div>Today</div>
        <div>Where It's Going</div>
      </div>
      <div class="table-row">
        <div>Version bumps</div>
        <div>Risk-aware decisions</div>
      </div>
      <div class="table-row">
        <div>Changelogs</div>
        <div>Outcome-based communication</div>
      </div>
      <div class="table-row">
        <div>CI automation</div>
        <div>Agent governance</div>
      </div>
      <div class="table-row">
        <div>Plugins</div>
        <div>Protocol ecosystem (MCP, CGP)</div>
      </div>
      <div class="table-row">
        <div>Single repo</div>
        <div>Multi-repo federation</div>
      </div>
      <div class="table-row">
        <div>Pass/fail gates</div>
        <div>Probabilistic risk scoring</div>
      </div>
    </div>
  </section>

  <!-- Who Relicta Is For -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Built For</p>
      <h2>Teams shipping at scale</h2>
    </div>
    <div class="feature-cards">
      <article class="card">
        <h3>Platform Engineers</h3>
        <p>Standardize release workflows across hundreds of services. One config, consistent governance, full observability. Monorepo support with independent, lockstep, and hybrid versioning.</p>
      </article>
      <article class="card">
        <h3>Release Managers</h3>
        <p>Regain visibility without slowing anyone down. Risk scores surface what needs attention; auto-approval handles what doesn't. Real-time dashboard with WebSocket streaming.</p>
      </article>
      <article class="card">
        <h3>Security & Compliance</h3>
        <p>Immutable hash-chain audit log. SLSA in-toto attestation with Sigstore signing. SBOM generation. OIDC/SSO with role-based access.</p>
      </article>
      <article class="card">
        <h3>Teams Adopting Agents</h3>
        <p>CGP protocol governs agent-initiated changes. Policy DSL defines what agents can auto-approve. Actor trust scores track reliability over time.</p>
      </article>
    </div>
  </section>

  <!-- MCP / AI Agents -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">AI-Native</p>
      <h2>The first release tool built for AI agents.</h2>
      <p class="lede">MCP is the industry standard for connecting AI agents to tools. Relicta is a native MCP server — not a wrapper, not a plugin.</p>
    </div>
    <div class="mcp-demo">
      <div class="mcp-conversation">
        <div class="mcp-message user">
          <span class="mcp-label">You</span>
          <p>"What's the release risk for this project?"</p>
        </div>
        <div class="mcp-message agent">
          <span class="mcp-label">Claude</span>
          <p>Risk score: 0.62 (medium). 3 breaking API changes detected across 2 packages. Blast radius: auth-service, api-gateway. Recommending v2.0.0 with 2-approver gate.</p>
        </div>
        <div class="mcp-message user">
          <span class="mcp-label">You</span>
          <p>"Approve and publish."</p>
        </div>
        <div class="mcp-message agent">
          <span class="mcp-label">Claude</span>
          <p>&check; v2.0.0 published. GitHub release created, Slack notified, in-toto attestation signed via Sigstore.</p>
        </div>
      </div>
    </div>
    <div class="mcp-tools-grid">
      <div class="mcp-tools-col">
        <h4>Tools (Agent Actions)</h4>
        <ul class="mcp-tool-list">
          <li><code>relicta.plan</code> Analyze commits & suggest version</li>
          <li><code>relicta.bump</code> Apply semantic version</li>
          <li><code>relicta.notes</code> Generate release notes</li>
          <li><code>relicta.evaluate</code> CGP risk evaluation</li>
          <li><code>relicta.approve</code> Governance gate</li>
          <li><code>relicta.publish</code> Execute release</li>
          <li><code>relicta.blast_radius</code> Impact analysis</li>
          <li><code>relicta.validate_release</code> Pre-flight checks</li>
        </ul>
      </div>
      <div class="mcp-tools-col">
        <h4>Resources (Agent Reads)</h4>
        <ul class="mcp-tool-list">
          <li><code>relicta://state</code> Release state machine</li>
          <li><code>relicta://config</code> Project configuration</li>
          <li><code>relicta://commits</code> Pending commits</li>
          <li><code>relicta://changelog</code> Generated changelog</li>
          <li><code>relicta://risk-report</code> Risk assessment</li>
        </ul>
      </div>
    </div>
    <div class="mcp-apps">
      <h3 class="mcp-apps-heading">Built-in MCP Apps</h3>
      <div class="mcp-apps-grid">
        <div class="mcp-app-card">
          <span class="mcp-app-icon">📊</span>
          <strong>Status Dashboard</strong>
          <p>Live release status at a glance</p>
        </div>
        <div class="mcp-app-card">
          <span class="mcp-app-icon">🚀</span>
          <strong>Release Pipeline</strong>
          <p>Step-by-step release progress</p>
        </div>
        <div class="mcp-app-card">
          <span class="mcp-app-icon">⚠️</span>
          <strong>Risk Assessment</strong>
          <p>Visual risk scoring breakdown</p>
        </div>
        <div class="mcp-app-card">
          <span class="mcp-app-icon">🔍</span>
          <strong>Commit Review</strong>
          <p>Classified commit analysis</p>
        </div>
        <div class="mcp-app-card">
          <span class="mcp-app-icon">✅</span>
          <strong>Approval Workflow</strong>
          <p>Interactive governance gates</p>
        </div>
        <div class="mcp-app-card">
          <span class="mcp-app-icon">💥</span>
          <strong>Blast Radius</strong>
          <p>Impact analysis across packages</p>
        </div>
      </div>
    </div>
    <div class="hero-actions" style="justify-content: center; margin-top: 24px;">
      <a class="btn solid" href="https://docs.relicta.tech/mcp">MCP Integration Guide</a>
    </div>
  </section>

  <!-- Open & Extensible -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Open Source</p>
      <h2>Open by design. Extensible by default.</h2>
    </div>
    <div class="grid">
      <div>
        <p class="lede">We believe the governance layer must be open. No vendor lock-in. Full transparency.</p>
        <div class="feature-list">
            <ul>
                <li>Open CLI (MIT Licensed)</li>
                <li>Plugin ecosystem (gRPC based, sandboxed)</li>
                <li>MCP server & interactive apps for AI agents</li>
                <li>Change Governance Protocol specification</li>
                <li>Protocol-first mindset</li>
            </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="section cta-band reveal-scale">
    <div>
      <h2>Start governing change today.</h2>
      <p class="lede">One binary. Zero cloud dependencies. Full governance trail.</p>
      <div class="cta-install">
        <code>brew install relicta-tech/tap/relicta</code>
      </div>
    </div>
    <div class="hero-actions">
      <a class="btn solid" href="https://docs.relicta.tech/install">Get started</a>
      <a class="btn ghost" href="https://docs.relicta.tech">Read the docs</a>
      <a class="btn ghost" href="/vision">Follow the vision</a>
    </div>
  </section>

</SiteLayout>

<style>
  /* Hero diagram */
  .abstract-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    padding: 40px;
    color: var(--color-terminal-text);
  }
  .diagram-node {
    border: 1px solid var(--color-border);
    padding: 12px 24px;
    border-radius: var(--radius-md);
    background: rgba(255,255,255,0.05);
  }
  .node-relicta {
    border-color: var(--color-accent);
    color: var(--color-accent);
    font-weight: bold;
    box-shadow: 0 0 15px rgba(92, 240, 200, 0.2);
  }
  .diagram-arrow {
    color: var(--color-muted);
    font-size: 1.2rem;
  }

  /* Numbers Bar */
  .numbers-bar {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1px;
    background: var(--color-border);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    margin: 32px 0;
  }
  .number-item {
    background: var(--color-surface);
    padding: 24px;
    text-align: center;
  }
  .number-value {
    display: block;
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 6px;
  }
  .number-label {
    font-size: 0.8rem;
    color: var(--color-muted);
    line-height: 1.3;
  }

  /* Pipeline */
  .pipeline {
    display: flex;
    align-items: flex-start;
    gap: 0;
    margin: 32px 0 24px;
    overflow-x: auto;
  }
  .pipeline-step {
    flex: 1;
    min-width: 140px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 16px;
    text-align: center;
  }
  .pipeline-step h4 {
    margin: 8px 0 4px;
  }
  .pipeline-step p {
    margin: 0;
    font-size: 0.78rem;
    color: var(--color-muted);
    line-height: 1.4;
  }
  .pipeline-icon {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--color-card);
    border: 1px solid var(--color-border);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 0.8rem;
    color: var(--color-muted);
  }
  .step-accent .pipeline-icon {
    background: rgba(92, 240, 200, 0.15);
    border-color: var(--color-accent);
    color: var(--color-accent);
  }
  .pipeline-connector {
    width: 24px;
    min-width: 24px;
    height: 2px;
    background: var(--color-border);
    align-self: center;
    margin-top: 20px;
  }
  .pipeline-terminal {
    margin-top: 0;
  }
  .pipeline-terminal pre {
    margin: 0;
    white-space: pre;
    overflow-x: auto;
  }
  .pipeline-terminal code {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    line-height: 1.6;
  }
  .t-muted { color: #5a6a7a; }
  .t-step { color: #6b8afd; font-weight: 700; }
  .t-accent { color: var(--color-accent); }
  .t-warn { color: var(--color-accent-warm); }

  /* Differentiators Grid */
  .diff-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-top: 32px;
  }
  .diff-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 24px;
    transition: border-color 0.2s ease;
  }
  .diff-card:hover {
    border-color: rgba(92, 240, 200, 0.3);
  }
  .diff-number {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-accent);
    font-weight: 700;
    margin-bottom: 8px;
  }
  .diff-card h3 {
    margin: 0 0 8px;
    font-size: 1.05rem;
  }
  .diff-card p {
    margin: 0;
    font-size: 0.88rem;
    color: var(--color-muted);
    line-height: 1.5;
  }

  /* Comparison Table */
  .compare-table-wrap {
    overflow-x: auto;
    margin-top: 24px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }
  .compare-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
  }
  .compare-table th,
  .compare-table td {
    padding: 12px 16px;
    text-align: center;
    border-bottom: 1px solid var(--color-border);
  }
  .compare-table th {
    background: var(--color-card);
    font-weight: 700;
    color: var(--color-text);
    white-space: nowrap;
  }
  .compare-table th:first-child,
  .compare-table td:first-child {
    text-align: left;
    font-weight: 600;
  }
  .compare-table .col-relicta {
    color: var(--color-accent);
  }
  .compare-table tbody tr:last-child td {
    border-bottom: none;
  }
  .compare-table .check { color: var(--color-muted); }
  .compare-table .dash { color: rgba(255,255,255,0.2); }
  .compare-table .accent { color: var(--color-accent); font-weight: 600; }

  /* Trust Grid */
  .trust-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-top: 24px;
  }
  .trust-item {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 20px;
  }
  .trust-item h4 {
    margin: 0 0 8px;
    font-size: 0.9rem;
  }
  .trust-item p {
    margin: 0;
    font-size: 0.82rem;
    color: var(--color-muted);
    line-height: 1.5;
  }

  /* MCP Tools Grid */
  .mcp-tools-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    max-width: 700px;
    margin: 32px auto 0;
  }
  .mcp-tools-col h4 {
    margin: 0 0 12px;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-muted);
  }
  .mcp-tool-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .mcp-tool-list li {
    padding: 6px 0;
    font-size: 0.82rem;
    color: var(--color-muted);
    border-bottom: 1px solid rgba(255,255,255,0.05);
  }
  .mcp-tool-list li:last-child {
    border-bottom: none;
  }
  .mcp-tool-list code {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--color-accent);
    margin-right: 6px;
  }

  /* CTA Install */
  .cta-install {
    margin: 16px 0;
  }
  .cta-install code {
    font-family: var(--font-mono);
    background: var(--color-card);
    border: 1px solid var(--color-border);
    padding: 10px 20px;
    border-radius: var(--radius-md);
    font-size: 0.9rem;
    display: inline-block;
  }

  /* Evolution table */
  .comparison-table {
    margin-top: 32px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
    overflow: hidden;
  }
  .table-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    padding: 16px 24px;
    background: var(--color-card);
    font-weight: 700;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
  }
  .table-header div:last-child {
    color: var(--color-accent-strong);
  }
  .table-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    padding: 16px 24px;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-muted);
  }
  .table-row:last-child {
    border-bottom: none;
  }
  .table-row div:last-child {
    color: var(--color-accent);
  }

  /* MCP Demo */
  .mcp-demo {
    max-width: 600px;
    margin: 32px auto 0;
  }
  .mcp-conversation {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 24px;
  }
  .mcp-message {
    margin-bottom: 20px;
  }
  .mcp-message:last-child {
    margin-bottom: 0;
  }
  .mcp-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 6px;
  }
  .mcp-message.user .mcp-label {
    color: #6b8afd;
  }
  .mcp-message.agent .mcp-label {
    color: var(--color-accent);
  }
  .mcp-message p {
    margin: 0;
    color: var(--color-text);
    font-size: 0.95rem;
    line-height: 1.5;
  }
  .mcp-message.user p {
    color: var(--color-muted);
    font-style: italic;
  }

  /* MCP Apps */
  .mcp-apps {
    max-width: 700px;
    margin: 40px auto 0;
  }
  .mcp-apps-heading {
    text-align: center;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-muted);
    margin-bottom: 20px;
  }
  .mcp-apps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }
  .mcp-app-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 16px;
    text-align: center;
  }
  .mcp-app-icon {
    display: block;
    font-size: 1.5rem;
    margin-bottom: 6px;
  }
  .mcp-app-card strong {
    display: block;
    font-size: 0.85rem;
    color: var(--color-text);
    margin-bottom: 4px;
  }
  .mcp-app-card p {
    margin: 0;
    font-size: 0.75rem;
    color: var(--color-muted);
    line-height: 1.3;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .numbers-bar {
      grid-template-columns: repeat(2, 1fr);
    }
    .diff-grid {
      grid-template-columns: 1fr;
    }
    .trust-grid {
      grid-template-columns: 1fr;
    }
    .pipeline {
      flex-direction: column;
      align-items: stretch;
    }
    .pipeline-connector {
      width: 2px;
      height: 16px;
      align-self: center;
      margin-top: 0;
    }
    .mcp-tools-grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 640px) {
    .abstract-diagram {
      flex-direction: column;
    }
    .diagram-arrow {
      transform: rotate(90deg);
    }
    .mcp-apps-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .numbers-bar {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
