---
import SiteLayout from "../layouts/SiteLayout.astro";

const title = "Relicta — The governance layer for software change";
const description = "Relicta governs change in an AI-driven world. From release automation to risk-aware decision making for humans and agents.";
---
<SiteLayout {title} {description}>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-text">
      <p class="eyebrow">The governance layer for software change</p>
      <h1>Governing change in an AI-driven world</h1>
      <p class="lede">As AI agents and CI systems generate more code, deciding what should ship becomes the hardest problem. Relicta governs change — before it reaches production.</p>
      <div class="hero-actions">
        <a class="btn solid" href="https://docs.relicta.tech/install">Get started with the CLI</a>
        <a class="btn ghost" href="/vision">Read the vision</a>
      </div>
    </div>
    <div class="hero-card">
      <div class="terminal">
        <!-- Abstract diagram placeholder: Agent / CI -> Relicta -> Production -->
        <div class="abstract-diagram">
          <div class="diagram-node">Agent / CI</div>
          <div class="diagram-arrow">→</div>
          <div class="diagram-node node-relicta">Relicta</div>
          <div class="diagram-arrow">→</div>
          <div class="diagram-node">Production</div>
        </div>
      </div>
      <div class="card-foot">The decision layer for modern software delivery.</div>
    </div>
  </section>

  <!-- The Problem -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">The Problem</p>
      <h2>Software delivery scaled. Decision-making didn’t.</h2>
      <p class="lede">Code is cheap. Changes are frequent. Risk is hidden. Trust erodes.</p>
    </div>
    <div class="grid">
      <div class="card">
        <h3>Past</h3>
        <p>Human-written code. Human reviews. Human pace.</p>
      </div>
      <div class="card">
        <h3>Now</h3>
        <p>Agent-generated change. CI automation. Overwhelmed humans.</p>
      </div>
      <div class="card card-featured">
        <h3>The Gap</h3>
        <p>Governance. How do we trust the machine to ship?</p>
      </div>
    </div>
  </section>

  <!-- What Relicta Is -->
  <section class="section reveal">
    <div class="grid">
      <div>
        <div class="section-heading">
          <p class="eyebrow">The Solution</p>
          <h2>Relicta governs change — not just releases.</h2>
        </div>
        <p class="lede">Relicta sits between humans, agents, and CI systems. It decides what changes, how risky it is, who approves, and how it’s communicated.</p>
      </div>
      <div class="feature-list">
        <div>
          <h4>The Core Decisions</h4>
          <ul>
            <li>What changed? (Semantic Analysis)</li>
            <li>Is it risky? (Risk Scoring)</li>
            <li>Who approves? (Governance)</li>
            <li>How to tell users? (Communication)</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- What Relicta Does Today -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Production Ready</p>
      <h2>Relicta is already a production-ready release governance CLI.</h2>
      <p class="lede">Thousands of releases governed. Zero data leaves your environment.</p>
    </div>
    <div class="grid">
      <div class="feature-list">
        <ul>
          <li><strong>Semantic version planning</strong> based on conventional commits</li>
          <li><strong>AI-assisted release notes</strong> using your own keys</li>
          <li><strong>Approval & audit trail</strong> for compliance</li>
          <li><strong>Plugin-based publishing</strong> (GitHub, npm, Docker...)</li>
          <li><strong>Local & CI-safe execution</strong> (Zero-install Action)</li>
        </ul>
        <div class="hero-actions">
           <a class="btn solid" href="https://docs.relicta.tech/install">Install Relicta</a>
           <a class="btn ghost" href="https://github.com/relicta-tech/relicta">View GitHub</a>
        </div>
      </div>
      <div class="terminal">
        <img src="/demos/marketing.gif" alt="Relicta CLI usage" loading="lazy" />
      </div>
    </div>
  </section>

  <!-- From Today -> Vision -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">The Evolution</p>
      <h2>From release automation to change governance</h2>
    </div>
    <div class="comparison-table">
      <div class="table-header">
        <div>Today</div>
        <div>Where It's Going</div>
      </div>
      <div class="table-row">
        <div>Version bumps</div>
        <div>Risk-aware decisions</div>
      </div>
      <div class="table-row">
        <div>Changelogs</div>
        <div>Outcome-based communication</div>
      </div>
      <div class="table-row">
        <div>CI automation</div>
        <div>Agent governance</div>
      </div>
      <div class="table-row">
        <div>Plugins</div>
        <div>Protocol ecosystem</div>
      </div>
    </div>
  </section>

  <!-- Who Relicta Is For -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Segments</p>
      <h2>Built for teams shipping at scale</h2>
    </div>
    <div class="feature-cards">
      <article class="card">
        <h3>Platform Engineers</h3>
        <p>Standardize release workflows across hundreds of services.</p>
      </article>
      <article class="card">
        <h3>Release Managers</h3>
        <p>Regain visibility and control without slowing down devs.</p>
      </article>
      <article class="card">
        <h3>Security & Compliance</h3>
        <p>Audit trails and approval gates built into the pipeline.</p>
      </article>
      <article class="card">
        <h3>Teams Adopting Agents</h3>
        <p>Safe guardrails for AI-generated code changes.</p>
      </article>
    </div>
  </section>

  <!-- Open & Extensible -->
  <section class="section reveal">
    <div class="section-heading">
      <p class="eyebrow">Open Source</p>
      <h2>Open by design. Extensible by default.</h2>
    </div>
    <div class="grid">
      <div>
        <p class="lede">We believe the governance layer must be open. No vendor lock-in. Full transparency.</p>
        <div class="feature-list">
            <ul>
                <li>Open CLI (MIT Licensed)</li>
                <li>Plugin ecosystem (gRPC based)</li>
                <li>Protocol-first mindset</li>
            </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="section cta-band reveal-scale">
    <div>
      <h2>Start governing change today.</h2>
      <p class="lede">Join the teams building the future of software delivery.</p>
    </div>
    <div class="hero-actions">
      <a class="btn solid" href="https://docs.relicta.tech/install">Get started</a>
      <a class="btn ghost" href="https://docs.relicta.tech">Read the docs</a>
      <a class="btn ghost" href="/vision">Follow the vision</a>
    </div>
  </section>

</SiteLayout>

<style>
  .abstract-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    padding: 40px;
    color: var(--color-terminal-text);
  }
  .diagram-node {
    border: 1px solid var(--color-border);
    padding: 12px 24px;
    border-radius: var(--radius-md);
    background: rgba(255,255,255,0.05);
  }
  .node-relicta {
    border-color: var(--color-accent);
    color: var(--color-accent);
    font-weight: bold;
    box-shadow: 0 0 15px rgba(92, 240, 200, 0.2);
  }
  .diagram-arrow {
    color: var(--color-muted);
    font-size: 1.2rem;
  }
  
  .comparison-table {
    margin-top: 32px;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-surface);
    overflow: hidden;
  }
  .table-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    padding: 16px 24px;
    background: var(--color-card);
    font-weight: 700;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
  }
  .table-header div:last-child {
    color: var(--color-accent-strong);
  }
  .table-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    padding: 16px 24px;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-muted);
  }
  .table-row:last-child {
    border-bottom: none;
  }
  .table-row div:last-child {
    color: var(--color-accent); 
  }
  @media (max-width: 640px) {
    .abstract-diagram {
      flex-direction: column;
    }
    .diagram-arrow {
      transform: rotate(90deg);
    }
  }
</style>